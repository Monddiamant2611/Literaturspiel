import { expect,test,type Page } from "@playwright/test";import { categoryLabels,learningTextTypes } from "../../src/data/textTypes";
async function reachPhase3(page:Page){await page.addInitScript(()=>{Math.random=()=>.5});await page.goto("/");await page.getByRole("button",{name:/Archiv öffnen/}).click();await page.getByRole("button",{name:"Gattungen entdecken"}).click();await page.getByRole("button",{name:"Verständnis prüfen"}).click();for(const answer of ["Epik","Lyrik","Dramatik"]){await page.getByRole("button",{name:answer,exact:true}).click();await page.getByRole("button",{name:/Nächste Aufgabe|Grundlagen abschließen/}).click()}await page.getByRole("button",{name:"Zu den Textsorten"}).click();for(let index=0;index<learningTextTypes.length;index++){await page.getByRole("button",{name:categoryLabels[learningTextTypes[index].category],exact:true}).click();await page.getByRole("button",{name:index===learningTextTypes.length-1?"Lernrunde abschließen":"Nächste Textsorte"}).click()}await page.getByRole("button",{name:/Das Archiv prüfen/}).click()}
test("existing recognition area follows genre introduction",async({page})=>{await reachPhase3(page);await expect(page.getByRole("heading",{name:"Welche Textsorte bin ich?"})).toBeVisible()});
for(const size of [{width:1440,height:900},{width:1024,height:768},{width:768,height:1024},{width:390,height:844}])test(`Phase 3 still fits ${size.width}x${size.height}`,async({page})=>{await page.setViewportSize(size);await reachPhase3(page);expect(await page.evaluate(()=>document.documentElement.scrollWidth===document.documentElement.clientWidth)).toBe(true)});
