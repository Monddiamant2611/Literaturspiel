import { expect,test } from "@playwright/test";
for(const size of [{width:1440,height:900},{width:390,height:844}])test(`genre overview has no horizontal overflow at ${size.width}`,async({page})=>{await page.setViewportSize(size);await page.goto("/");await page.getByRole("button",{name:/Archiv öffnen/}).click();await page.getByRole("button",{name:"Gattungen entdecken"}).click();await expect(page.getByRole("heading",{name:"Dramatik – Literatur für die Aufführung"})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth===document.documentElement.clientWidth)).toBe(true)});
