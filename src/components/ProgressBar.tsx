export function ProgressBar({ value,max,label }: { value:number;max:number;label:string }) { const width=Math.max(0,Math.min(100,value/max*100)); return <div className="progress"><div className="progress__meta"><span>{label}</span><span>{value} von {max}</span></div><div className="progress__track" role="progressbar" aria-label={label} aria-valuemin={0} aria-valuemax={max} aria-valuenow={value}><span style={{width:`${width}%`}} /></div></div>; }
